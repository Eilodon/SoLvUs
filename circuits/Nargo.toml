[package]
name = "solvus"
authors = ["Eidolon-V"]
type = "bin"

[dependencies]
legacy_poseidon = { path = "vendor/legacy_poseidon" }
sha256 = { tag = "v0.2.1", git = "https://github.com/noir-lang/sha256" }
